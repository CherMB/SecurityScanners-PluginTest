pipeline {
    agent any

    stages {
        stage('List Workspace Contents') {
            steps {
                echo '📁 Listing contents of the workspace...'
                sh 'ls -lrth'
            }
        }
    }
}
